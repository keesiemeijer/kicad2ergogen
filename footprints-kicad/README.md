# KiCad footprints

This directory contains KiCad `.kicad_mod` source footprints used as input for conversion.

Most generated or imported footprint files in this directory are intentionally ignored by Git. Keep `README.md` committed to document the directory purpose.
